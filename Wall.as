/*	Filename:	Wall.as		Purpose:	Wall graphics container		Author:		Justin Bachorik		Date:		03/20/09	*/package {		import flash.display.*;	import flash.text.*;	import flash.net.*;	import flash.events.*;	import caurina.transitions.Tweener;		public class Wall extends Sprite {				// constants		private static const IMAGE_LOCATION = 'img/wall.png';				// instance variables				// the bitmap loader		private var imageLoader:Loader;				// constructor		public function Wall():void {						// instantiate the loader			imageLoader = new Loader();						// set up the event listener for load completion			imageLoader.contentLoaderInfo.addEventListener(Event.INIT, initListener);						// load the bitmap			imageLoader.load(new URLRequest(IMAGE_LOCATION));					} // end constructor				// handler to display the loaded image		private function initListener(e:Event):void {			this.addChild(imageLoader.content);					}					}	// end class	}	// end package