/*	Filename:	PowerUp.as		Purpose:	PowerUp graphics container and information storage class		Author:		Justin Bachorik		Date:		03/20/09	*/package {		import flash.display.*;	import flash.text.*;	import flash.net.*;	import flash.events.*;	import caurina.transitions.Tweener;		public class PowerUp extends Sprite {				// constants		private static const IMAGE_LOCATION = 'img/powerup';		private static const IMAGE_FORMAT = '.png';				// instance variables		private var myStrength:Number;				// the bitmap loader		private var imageLoader:Loader;				// constructor		public function PowerUp(strength:Number):void {						// instantiate the loader			imageLoader = new Loader();						// set up the event listener for load completion			imageLoader.contentLoaderInfo.addEventListener(Event.INIT, initListener);						// load the bitmap			imageLoader.load(new URLRequest(IMAGE_LOCATION+strength+IMAGE_FORMAT));						myStrength = strength;					} // end constructor				// handler to display the loaded image		private function initListener(e:Event):void {			this.addChild(imageLoader.content);		}				// public accessor method for strength		public function getStrength():Number {			return myStrength;		}	// end getStrength					}	// end class	}	// end package